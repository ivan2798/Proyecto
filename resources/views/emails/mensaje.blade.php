<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<body>
    <h2>Contendientes</h2>

    <p>Estimando(a) {{ $ }}</p>
    <p>
        Se le notifica que su proyecto <b>{{ $ }}</b>
        fue aprovado.
    </p>
</body>
</html>